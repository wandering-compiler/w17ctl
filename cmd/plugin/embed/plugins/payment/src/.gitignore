# Standard Go + IDE artifacts. NOTE: gen/pb/ is COMMITTED — do not
# add it here (it's the author dev/test loop's build-against stub,
# regenerated by `w17ctl plugin gen-pb`).
*.test
*.out
coverage.txt
.idea/
.vscode/
